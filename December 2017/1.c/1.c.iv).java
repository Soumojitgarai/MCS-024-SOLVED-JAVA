// 1. Declare and initialize with values in curly braces:
int[] numbers = {1, 2, 3, 4, 5};

// 2. Declare and then initialize using a loop:
int[] squares = new int[5];
for (int i = 0; i < squares.length; i++) {
    squares[i] = i * i;
}

// 3. Declare and initialize with default values based on type:
boolean[] flags = new boolean[10]; // All elements will be false by default

// 4. For strings, each character is an element:
String[] names = {"Alice", "Bob", "Charlie"};

